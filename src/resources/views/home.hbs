<div class="mt-5">
    <div class="row">
        {{#each courses}}
            <div class="col-sm-6 col-lg-4">
                <div class="card" style="width:20rem;margin:20px 0 24px 0">
                    <img class="card-img-top" src="{{this.image}}" alt="image" style="width:100%">
                    <div class="card-body">
                        <h4 class="card-title">{{this.title}}</h4>
                        <p class="card-text">{{this.description}}</p>
                        <a href="javascript:void(0)" class="btn btn-primary">Chi tiết</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>